export class Client {
    constructor(private num?: String,
        private prenom?: String,
        private nom?: String,
        private adresse?: String,
        private responsable?: String,
        ) {
    }
}
